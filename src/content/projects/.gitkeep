The `projects` content collection is SCHEMA-ONLY in v1 (see ../SCHEMA.md > "Next step:
the projects migration"). No entries yet; the polished case pages in src/pages/work/
still own project rendering. This directory exists so the glob loader has a base dir.
