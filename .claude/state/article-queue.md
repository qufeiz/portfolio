# Article queue — the writing backlog

The `write-article` ref draws the NEXT article from here. **But the queue is not read alone:** the
writer FIRST reads `state/sources.md` + `state/coverage.md` + this file (all cheap), picks a queued
item whose source aspect is still **Open** in `coverage.md`, writes ONE article, then updates the
ledger (move the row to Done here, flip the aspect to Covered in `coverage.md`, append a detailed
entry to `state/log.md`). It does NOT re-scan every project + article each run — the ledger is its
memory.

> **Honesty + confidentiality gate** (see `sources.md` › HARD GATES and `AGENTS.md` › Gates): no
> invented metrics, collaborations credited, FredGPT confidentiality respected, never blog a
> `skip-confidential` / `skip-PII` / `skip-not-mine` source. Ground each piece in REAL work + docs.

Each row: **slug · title · category · source · one-line angle.** `category` is the frontmatter
`category` (`agent-systems` | `projects` | `life` | `essays`) per `src/content/SCHEMA.md`.

## Already drafted — MIGRATED into the KB (done; do NOT redo)
- [x] `orchestrate-gate-ratchet` — **Orchestrate → Gate → Ratchet** · `agent-systems` → `/notes/orchestrate-gate-ratchet`
- [x] `independent-verification` — **Independent verification** · `agent-systems` → `/notes/independent-verification`
- [x] `the-ratchet` — **The ratchet** · `agent-systems` → `/notes/the-ratchet`

---

## Queue — write/refresh ONE per loop fire (top = next). Check `coverage.md` for the OPEN aspect.

### Concept posts (`category: agent-systems`) — the cross-cutting ideas
- (none open — the concept queue is drained; see Done)

### Nucleus mechanism deep-dives (`category: projects`) — CLIENT WORK, MECHANISM ONLY (see `sources.md` › Nucleus gate). Ground each in `~/Projects/nucleus` `main` code; client NEVER named; DRAFT until owner OKs the bar.
- (none open — the queued Nucleus mechanism is drained; see Done. New mechanisms get queued as they ship on `main`.)

### Needs owner input before writing
- [ ] `ychack` — **YC hackathon build** · `projects` · `portfolio.zip › YChack` · only 3 image assets in the zip (no code/text) — owner must describe what was actually built before this can be written honestly.

## Done
- [x] `nucleus-scale-to-zero` — **Scale to zero: an always-off demo that wakes on request** · `projects` (`kind: concept`, order 15) · Nucleus (CLIENT WORK, MECHANISM ONLY) · cost engineering for a low-traffic production AI app: make it cost near-nothing at rest without a cold start that feels broken. Two grounded mechanisms: (1) Fly scale-to-zero — `fly.toml` `auto_stop_machines="suspend"` + `auto_start_machines=true` + `min_machines_running=0`, so the engine is off by default and auto-resumes on the next request, billing only while actually serving; `suspend` (memory-snapshot resume) is chosen over `stop` (full cold boot) so the first ask after a long idle reads as a small delay, not a broken demo (the app runs the Next.js standalone server + an agent subprocess, so keeping the initialized image around is exactly where suspend earns its keep); concurrency limits stay configured so a real burst still fans out. (2) Model-aware cost/usage metering — the inspector's cost line was hardcoded to one model's stale rates and labeled every call the cheap model (under-reporting the expensive one several times over, corrupting any spend ledger built on it); the fix prices each call by the RESOLVED model via a per-model price table (`answer-agentic.ts`), local models price at zero, and every answer engine feeds its resolved model in; the `buildCost` path (`answer.ts`) is honest-by-construction — cost=null-with-a-reason (never a guessed figure) when a provider doesn't report tokens or a model's price isn't tracked; and the end-to-end lifecycle journey appends one measured USAGE row per SDK call (resolved model + real reported in/out token counts + computed cost), defensively so a logging failure never reddens the run — so cost is measured, not estimated. Grounded EXCLUSIVELY in `~/Projects/nucleus` `main` commits `07a2f59` (`fly.toml`) + `4f5234d` (`src/lib/engine/answer-agentic.ts`, `answer.ts` `buildCost`, `answer-local.ts`, `answer-messages.ts`, `tests/journeys/lifecycle.mjs`). Client NEVER named; ZERO dollar figures / token counts / usage totals / credit balances / per-model rate numbers (code excerpts sanitized to placeholders; real absolute call-log path genericized); NO Fly app name / hostname / region; `82d31f4` (client-runbook commit) deliberately NOT read; no client-comms/rebrand-branch touched; the commit MESSAGES' own dollar figures + contact name deliberately excluded. Wired rendering: 1 mermaid (idle→wake→serve→idle cycle), 2 callouts (`:::note` why suspend-not-stop, `:::tip` an honest cost line refuses to guess), 3 annotated fences (`toml` fly config, `ts` model-aware pricing, `ts` honest buildCost, `js` logUsage). No em-dashes/arrow glyphs anywhere (code excerpts cleaned too — no verbatim-quote exception needed). Cross-linked to `/notes/honest-automation`, `/notes/nucleus-byo-keys`, `/notes/nucleus-local-mode`. NORMAL publish (owner APPROVED the Nucleus confidentiality bar; NOT draft) — built + logged, lead does the hard confidentiality + no-dollar-figures sweep + deploys. → `/notes/nucleus-scale-to-zero`
- [x] `nucleus-local-mode` — **Local mode: answers without the data leaving your machine** · `projects` (`kind: concept`, order 14) · Nucleus (CLIENT WORK, MECHANISM ONLY) · a privacy posture for a multi-tenant AI assistant: one per-owner `model_mode` setting reroutes the whole answer path through the user's OWN local model so questions/documents never leave the machine, from one codebase (no fork). Grounded mechanisms: (1) the switch is a per-owner `model_mode` resolved at request time by `getModelMode` that FAILS SAFE — anything not the literal `local`/`hipaa` → `cloud`, so a blank/corrupt value can't half-route to an unwired backend; (2) ONE dispatch point in `/api/ask` — `local` runs `answerLocal` IN-PROCESS and is NEVER forwarded to the hosted Fly agent (so a Local question physically can't take the cloud route), while `cloud` stays byte-for-byte the prior behavior (one product, not two that drift); `hipaa` returns an honest 503 rather than a silent downgrade; (3) `answerLocal` round-trips the question through the owner's OWN OpenAI-compatible endpoint (Ollama; `local_endpoint`/`local_model` per-owner settings) — the only outbound call is to the user's own address, no hosted provider, placeholder bearer (no billed key); (4) honest v0.5 scope — chat-only, `grounded:false`, zero citations, `model:local:<name>` attribution, a prompt-level guardrail that NAMES in-scope files but FORBIDS fabricating their contents; (5) never-500 friendly `localGuidance` results for unset/unreachable endpoint (names the address, fails in seconds) + a `$0` cost overwrite (owner's hardware, no paid API). Verified LIVE by journey `local-mode.mjs` (real browser + box Ollama, ZERO Claude spend: selects Local through the real UI, Test connection, genuine round-trip asserting `model` starts with `local:`, honest no-docs refusal with `grounded===false` + zero citation chips, clean restore). Grounded EXCLUSIVELY in `~/Projects/nucleus` `main` commit `0c19d58` (`src/lib/engine/answer-local.ts`, `src/lib/engine/settings.ts` `getModelMode`, `src/app/api/ask/route.ts`, `src/components/models-panel.tsx`, `docs/LOCAL-MODEL.md`, `tests/journeys/local-mode.mjs`). Client NEVER named; ZERO endpoints/hostnames/IPs/secrets/client figures; `82d31f4` (client-runbook commit) deliberately NOT read; no client-comms/rebrand-branch touched. Wired rendering: 1 mermaid (model_mode lane dispatch), 3 callouts (`:::note` local≠fully-offline, `:::tip` why grounded=false is honest, plus the in-flow honesty), 6 annotated `ts` fences quoting real code. The only em-dash/arrow glyphs are inside a VERBATIM real-code quote (the `unreachableResult` answer string, `answer-local.ts:237`) — the one allowed exception; prose/deck/keyTakes are clean. Cross-linked to `/notes/nucleus-byo-keys`, `/notes/nucleus-multi-tenant-isolation`, `/notes/honest-automation`. NORMAL publish (owner APPROVED the Nucleus confidentiality bar 2026-07-24; NOT draft) — built + logged, lead does the hard confidentiality sweep + deploys. → `/notes/nucleus-local-mode`
- [x] `nucleus-byo-keys` — **Bring-your-own API keys, verified without spending a token** · `projects` (`kind: concept`, order 13) · Nucleus (CLIENT WORK, MECHANISM ONLY) · a multi-tenant AI app where each user brings their OWN model key instead of silently riding a shared one. Three grounded mechanisms: (1) ONE credential resolver (`anthropic-auth.ts` `resolveAnthropicAuth`: own -> house -> none, fail-CLOSED on settings errors so a DB hiccup never fails open onto the shared key) shared by both answer engines, returning the honest answer-shaped `noKeyAnswer` (grounded=false, ZERO upstream calls) for an unflagged keyless owner + the agentic engine's per-request `ownKeyEnv` SDK-subprocess env injection (never mutates process.env); (2) the shared "house" key as an admin-only ALLOWLIST — `house_key_enabled="1"` written SOLELY from the admin Users route (`setHouseKey`, admin-guarded, not self-grantable), grandfathered additively by migration 018 (`insert ... on conflict do nothing`); (3) write-only key storage (server keeps only name/last4/created, never returns the secret) + a ZERO-TOKEN "Test connection" that validates a pasted key with `client.models.list()` (billed-nothing metadata call, NEVER `messages.create`) via the SAME resolver, with key-shaped error redaction + no-500-on-bad-key. Grounded EXCLUSIVELY in `~/Projects/nucleus` `main` commits `c1ad91b` + `87e488b` (`src/lib/engine/anthropic-auth.ts`, `settings.ts`, `answer-agentic.ts`, `answer-messages.ts`, `src/app/api/settings/route.ts`, `api/admin/users/route.ts`, `api/test-connection/route.ts`, `supabase/migrations/018_house_key_allowlist.sql`). Client NEVER named; ZERO key material / house-key value / allowlist contents / client figures; `82d31f4` (client-runbook commit) deliberately NOT read; no client-comms/rebrand-branch touched. Wired rendering: 1 mermaid (credential-resolution flow), 2 callouts (`:::note` additive grandfathering, `:::tip` why models.list not a one-word message), 6 annotated `ts` fences quoting real code. Cross-linked to `/notes/honest-automation`. DRAFT (built + logged, NOT deployed — joins the owner confidentiality review batch). → `/notes/nucleus-byo-keys`
- [x] `nucleus-multi-tenant-isolation` — **Keeping every tenant's documents private** · `projects` (`kind: concept`, order 12) · Nucleus (CLIENT WORK, MECHANISM ONLY) · how a multi-tenant RAG assistant isolates each user's uploaded documents at the DATA layer. Three grounded layers: (1) owner-scoped storage path `<owner_id>/<sha256(docId)>` with the fail-closed delete-path assertion (`path.startsWith('${ownerId}/')` or refuse); (2) scope-qualified doc ids — `docIdFromFilename(name, scope)` suffixes 12 hex of `SHA-256(scope)` so `report.pdf` in chat A and chat B are DIFFERENT documents (closes the cross-chat filename steal, bug F4), while a same-scope re-upload still replaces; (3) the per-owner `withManifestLock` settle-safe promise chain serializing `_files.json` read-modify-writes so concurrent upload/upload + upload/delete don't lose an entry (bug F5), with the cross-instance limitation + the Postgres-table durable fix stated honestly in a `:::warning`. Grounded in `~/Projects/nucleus` `main` commit `cb33230` (`src/lib/engine/doc-files.ts`, `ingest.ts`, `src/app/api/ingest/route.ts`) + the RED-first `doc-files-collision.test.mts` / `doc-files-race.test.mts`. Client NEVER named; ZERO client-comms/rebrand-branch/secrets referenced; `82d31f4` (client-runbook commit) deliberately NOT read. Wired rendering: 1 mermaid lock-serialization flow, 2 callouts (`:::note` why-not-the-prompt, `:::warning` what-the-lock-does-not-close), 5 annotated `ts` fences quoting real code. Cross-linked to `/notes/honest-automation`. DRAFT (built + logged, NOT deployed — awaits owner confidentiality review). → `/notes/nucleus-multi-tenant-isolation`
- [x] `packaging-an-agent` — **Two ways to package an agent** · `agent-systems` (`kind: concept`, order 8) · this portfolio repo (shape A) + the build team (shape B) · the SAME orchestrate-then-delegate spine in two packagings. Shape A = repo-level capability model: each capability is a two-file unit (`.claude/agents/<name>.md` = a persona + a pointer that says "Read and follow `.claude/refs/<name>.md` exactly", and the ref = the full numbered procedure); the thin lead loop in `AGENTS.md` DISCOVERS agents at runtime (`ls .claude/agents/*.md`) and spawns by name with a one-line goal; growing = add a def + ref (the capability ratchet); versioned with the code. Grounded in the three REAL agents (`portfolio-writer`, `portfolio-refresh`, `make-video`) each pointing at a matching ref. Shape B = the standing PM/engineer/verifier team (`~/.claude/agents/`): roles + design-approval gate (human-approved before any code) + independent verifier (read-only, did not write the code). Names the tradeoffs honestly (A cheap/repeatable-procedure-only; B carries the gate+grader overhead, overkill on a small job; not exclusive). Cross-linked to `/notes/my-agent-teams`, `/notes/context-engineering`, `/notes/orchestrate-gate-ratchet`, `/notes/independent-verification`. MECHANISM only, no invented metrics, owner's own systems. → `/notes/packaging-an-agent`
- [x] `honest-automation` — **Honest automation** · `agent-systems` (`kind: concept`, order 7) · x-agent + this portfolio agent + jobright-agent · agents that ship real, unsupervised work without overclaiming. Thesis: autonomy is safe only when the agent is built to fail closed and to report precisely what it did and did NOT verify. Grounded in REAL code: x-agent `privacy_scan.py` (FAIL-CLOSED, `--shareable` DEFAULT-DENY, exit 2 when the protective name set loads empty on a non-empty wiki — "empty means mis-pointed, not safe"); `tweet_composer.py` (validator not generator, exit 1 on AI tells, `--selftest` gates the gate); `x-post-playwright.mjs` (`POST STATUS: POSTED | UNCONFIRMED | ERROR` from the read-back, idempotent by url in `posted.jsonl`); this portfolio agent's `AGENTS.md` (honesty floor = skip rather than filler, the lead's INDEPENDENT step-2 gate before publish, deploy-verify = curl the live Pages URL, "never claim a deploy you can't see"); jobright-agent (never-auto-submit human gate, two-channel honest-status ladder confirmed/submitted-no-email/unverified/failed, CAPTCHA-is-a-hard-stop, never-invent-personal-data). MECHANISM ONLY — no credentials/cookies/handles/PII. Cross-linked to `/notes/determinism-around-stochasticity`, `/notes/independent-verification`, `/notes/x-agent`, `/notes/jobright-agent`, `/notes/my-agent-teams`. → `/notes/honest-automation`
- [x] `compiled-knowledge` — **Compiled knowledge and the self-ingest loop** · `agent-systems` (`kind: concept`, order 6) · `life-wiki` (the SYSTEM only) · knowledge as a COMPILED, regenerable artifact, not a transcript pile: a deterministic local extractor (zip → per-conversation markdown + a priority-scored queue, no LLM) feeds an LLM ingest loop that triages, resolves canonical-claim dedup (extend-not-fork, ambiguous → human review), dates by CONTENT not export (with confidence levels), appends a dated stance-log on a changed mind (never overwrites), and runs `regen.py` to rebuild the by-type index + timeline + coverage report as BUILD OUTPUTS from page frontmatter. The loop runs each batch in a throwaway subagent (heavy transcripts die there); the queue makes it resumable. Karpathy's LLM-wiki idea CREDITED as inspiration, never claimed; the IMPLEMENTATION is the owner's. MECHANISM ONLY — zero private corpus content (no `raw/`, no `wiki/` pages, no `index/`/`log.md`/vault). Cross-linked to `/notes/context-engineering` + `/notes/my-agent-teams`. → `/notes/compiled-knowledge`
- [x] `context-engineering` — **Bounded context, unbounded work** · `agent-systems` (`kind: concept`, order 5) · this repo's own `.claude/AGENTS.md` (the lead that holds the plan and does NO grunt work, spawns named worker subagents pointed at `.claude/refs/*.md`, the disposable-context rationale, the request→fulfill handoff that exists because a worker subagent cannot spawn another subagent) + the build team (pm/engineer/verifier fan-out) + the fleet's throwaway-worker pattern (jobright apply-subagent, x-agent post subagent) + the writer loop itself as the live example. Thesis: a bigger window doesn't fix context rot; the fix is architectural — keep the plan in a small long-lived context, do all grunt work in bounded throwaway subagents, request (don't spawn) heavy follow-on so the hierarchy stays flat. Cross-linked to `/notes/orchestrate-gate-ratchet`, `/notes/my-agent-teams`, `/notes/jobright-agent`, `/notes/x-agent`. → `/notes/context-engineering`
- [x] `smartwatch-user-research` — **What people actually want from a smartwatch (and a smart dumbbell)** · `projects` · `portfolio.zip › SmartWatch用户调研` · a CMU smart-fitness-equipment course project written up as a two-phase UX/research artifact: discovery (secondary research on sedentary work + 10 spontaneous in-person interviews → the "sit 6-10h, know it's bad, push through" finding, the age-split behavior, and "nobody wanted a tracker, they wanted built-in movement") and a build/test (a CPX smart-dumbbell prototype, water-bottle simulated, chosen because hands-on-weight forces hands-free detection; an 8-participant think-aloud test where the no-buttons interaction held but classification of similar lifts (bicep vs hammer curl) failed and participants silently changed their own form to be read). Honest coursework framing, no invented metrics; interviewee/participant names + employers anonymized to role + age band. → `/notes/smartwatch-user-research`
- [x] `startup-experiments` — **Things I tried to start** · `projects` · `portfolio.zip › Startup` · early-venture experiments led by zoning-copilot: real FastAPI + Playwright + OpenAI Python that answers a California zoning question by walking an LLM down the live Municode TOC (no vector store) and answering only from the fetched code sections with citations; the crawler runs `fetch()` inside the authenticated browser session; a built-but-unshipped `planner.py`/`rag.py` indexing path noted honestly. Supporting (scoped honestly): AppraisalOS (a technical design doc, not code), n8n (screenshots only), wordpressseo (a real 29-node n8n RSS → OpenAI-translate → Telegram-approval → WordPress-draft workflow). Through-line: LLM/automation proposes, human/hard-rule disposes. → `/notes/startup-experiments`
- [x] `determinism-around-stochasticity` — **Determinism around stochasticity** · `agent-systems` · feature-team + the fleet's gates · an LLM call is a sample from a distribution, so you never trust one run, you gate it with deterministic code: the engineer's pure `validateX()` (passes every golden, fails every toy), the verifier's removable-handler proof (break the handler → red), x-agent's rules-as-data voice gate (`tweet_composer.py`, exit 1 on a tell, `--selftest`), and the fail-closed `privacy_scan.py` (default-deny `--shareable`; empty rule set → exit 2, never 0). Closes with "the LLM proposes, the deterministic layer disposes" + the meta-miss link to the ratchet. Cross-linked to `orchestrate-gate-ratchet` / `independent-verification` / `the-ratchet`. → `/notes/determinism-around-stochasticity`
- [x] `x-agent` — **Posting to X, headless, in my own voice** · `projects` · `x-agent` · two skills drive a real X account end-to-end via Playwright + a seeded cookie session (X blocks headless logins), behind a deterministic de-AI voice gate (the same no-em-dash/no-AI-tells floor this writer mirrors) that fails closed on AI tells; original posts add a fail-closed privacy gate + meaning-layer self-check + human approval; the post script reads the result back and logs an honest POSTED/UNCONFIRMED/ERROR status, idempotent by url. Mechanism only: no credentials, no seeded cookies, no account handle, no personal posts, no fabricated counts. Cross-link: this is the downstream consumer of every article's keyTakes. → `/notes/x-agent`
- [x] `jobright-agent` — **An agent that applies to jobs while I sleep** · `projects` · `jobright-agent` · end-to-end Jobright application automation packaged as the `jobright-apply` skill: orchestrator + throwaway apply-subagents, the never-auto-submit human gate, two-channel verification with honest statuses, the ATS dispatcher with walled-platform bailout, and the React-typing / spam-detection / rotating-code gotchas. PII held (no real name/contact/employers). → `/notes/jobright-agent`
- [x] `econorag` — **EconoRAG: a ReAct agent over Federal Reserve data** · `projects` · `econorag-frontend`+`my-langgraph-rag` · a LangGraph ReAct agent that answers economics questions only from live FRED tool calls + a user-scoped retrieved index (never general world knowledge); 7 tools, a 4-call cap, out-of-band chart attachments; predecessor of the FredGPT line. → `/notes/econorag`
- [x] `contract-retriever` — **From RAG to an agent that reads the files** · `projects` · `Contract-Retriever-RAG`+`-Agentic` · the client rejected vanilla vector search, so v1 was query-routing + hybrid SQL/RAG and v2 a Claude Agent SDK loop that navigates a `knowledge/` tree with no embeddings; same product, swapped retrieval engine. → `/notes/contract-retriever`
- [x] `my-agent-teams` — **The hub: "My Agent Teams"** · `agent-systems` (`kind: hub`, order 0) → `/notes/my-agent-teams`. The two teams (build team + fleet) + the orchestrate→gate→ratchet thesis; links the concept articles. Deeper per-team sections marked to-be-expanded.
- [x] `orchestrate-gate-ratchet` → `/notes/orchestrate-gate-ratchet` (migrated to KB)
- [x] `independent-verification` → `/notes/independent-verification` (migrated to KB)
- [x] `the-ratchet` → `/notes/the-ratchet` (migrated to KB)
