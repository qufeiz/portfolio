# Portfolio-agent log — append-only

Every loop fire, ratchet, and structural change appends ONE entry here. Headers are
grep-parseable: `## YYYY-MM-DD <TYPE> | <one-line>`. Types:

- `init`       — the system was created / re-scaffolded
- `loop-run`   — a weekly loop fired (what the subagents did, what shipped)
- `loop-adjust`— the ratchet: a subagent fell short, a ref (`.claude/refs/<name>.md`) was edited to fix it
- `ref-add`    — a new capability ref was added to `.claude/refs/` (the capability ratchet)
- `deploy`     — a publish to GitHub Pages + the live-verify result

Newest at the bottom. Grep examples:
`grep '^## .* loop-adjust' state/log.md` · `grep '^## .* deploy' state/log.md`

---

## 2026-06-14 init | v1 scaffold: lead orchestrator (AGENTS.md) + portfolio-refresh + portfolio-write capabilities + state
- Created `.claude/AGENTS.md` (lead runbook + the canonical weekly loop prompt + gates + "how to add a capability").
- Created the two v1 capabilities: `portfolio-refresh` + `portfolio-write` (originally `skills/<name>/SKILL.md`; recast to `.claude/refs/<name>.md` flat how-to docs — see the recast entry below).
- Seeded state: `cursor.json` (HEADs of TreAxe, x-agent, life-wiki, jobright-agent, feature-team, portfolio; FredGPT source = null/not-a-repo, see `FREDGPT.zip`), `article-queue.md` (6 article ideas), `roadmap.md` (4 planned capabilities).
- Seed content noted: 3 drafted articles (then in a since-removed build scratch dir, now migrated into `src/content/articles/`) (00,01,02) to be wired into the site's "Agent Systems" section.
- Loop NOT run; nothing deployed. This is scaffolding/architecture — specifics to be improved later.

## 2026-06-14 loop-adjust | capability model recast from "skills" to "refs" (simpler, easier to update)
- Capabilities are now flat "how-to" REF FILES at `.claude/refs/<name>.md` run via generic throwaway subagents — not Claude skills / agent-types. Matches the rest of the owner's fleet (point a subagent at a ref/runbook).
- Moved `skills/portfolio-refresh/SKILL.md` → `.claude/refs/refresh-portfolio.md`; `skills/portfolio-write/SKILL.md` → `.claude/refs/write-article.md` (all instructional content preserved; write-article still targets `src/content/articles/<slug>.md` per `src/content/SCHEMA.md`). Removed the now-empty `.claude/skills/`.
- Reframed `AGENTS.md` (architecture + loop step 0 = `ls .claude/refs/*.md`, step 1 = spawn one subagent per ref with the "Read and follow `.claude/refs/<name>.md` exactly" brief), `roadmap.md` (planned capabilities = future ref files), and the `skill-add` ledger type → `ref-add`. Adding a capability = write one ref file; updating = edit it.

## 2026-06-14 loop-adjust | capability model evolved to project-level subagent DEFS + refs (matches the owner's build team)
- Capabilities are now **project-level subagents** in `.claude/agents/<name>.md` (Claude Code subagent format: YAML `name`/`description`/optional `tools` + a short body = system prompt that carries the persona + the ref pointer), each pointing at its `.claude/refs/<name>.md` how-to. Mirrors the owner's `pm`/`engineer`/`verifier` defs+refs pattern. The lead now spawns a NAMED subagent (`subagent_type: <name>`) with a HIGH-LEVEL goal, not the verbatim "read this ref" brief — the def already tells the worker its job + which ref to read.
- Created `.claude/agents/portfolio-refresh.md` (→ `refs/refresh-portfolio.md`) + `.claude/agents/portfolio-writer.md` (→ `refs/write-article.md`). Refs KEPT as-is (only their top "who points at this" blurb updated to name the worker; the how-to content is unchanged; write-article still targets `src/content/articles/<slug>.md` per `src/content/SCHEMA.md`).
- Reframed `AGENTS.md`: added the 3-layer naming clarifier (AGENTS.md = LEAD loop · `.claude/agents/` = WORKER subagents · `.claude/refs/` = the how-tos), loop step 0 = `ls .claude/agents/*.md`, step 1 = spawn the named subagent with a one-line goal (+ note that the loop can spawn these because they're project-level types, run from a top-level session). `roadmap.md` planned capabilities = future agent defs + refs. `npm run build` sanity check passed; nothing deployed.

## 2026-06-14 loop-adjust | split the operating model: DAILY writer-only loop + ON-DEMAND scanner (was: loop spawned BOTH each fire)
- The recurring loop is now **WRITER-ONLY and DAILY** — one article per fire. Rewrote the `AGENTS.md` loop: (0) read ledger (coverage+queue+log, never re-scan), (1) spawn ONE `portfolio-writer` with a high-level "write the next highest-value OPEN aspect / next queued item" goal, (2) gate (honesty/confidentiality/build), (3) deploy + verify live, (4) ratchet on a miss, (5) update coverage/log + report 2-3 lines, STOP until tomorrow. Added an HONESTY FLOOR (skip the day with no filler if no real OPEN aspect) and a DRAFT-MODE toggle (write+build+log, skip deploy; default = auto-publish). Cadence/run note updated to `/loop 1d` or daily cron, FROM the repo root.
- The SCANNER (`portfolio-refresh`) is now **on-demand, NOT in the loop** — added an "On-demand: refresh the site from projects" section (owner says "explore and update the portfolio" → read ledger → scan since cursor → update site → gate → deploy → update coverage/log). Rationale stated: projects are added rarely/deliberately, so daily re-scans are wasteful.
- Preserved: capabilities = agents+refs, discover-at-runtime, the gates table, the in-repo `.scratch/` convention, run-from-repo-root, the two-level memory (coverage index + action log). Generalized "how to add a capability" to route a new agent to the writer-loop (writer-class) vs on-demand (scanner/site-class) trigger. Mirrored the two-trigger note into `state/coverage.md`. No build, no deploy (a parallel pass owns `src/content/articles/`).

## 2026-06-15 ref-add | built `make-video` capability (Hyperframes screenshot-walkthrough → MP4) + the request→fulfill handoff
- New project-level subagent: `.claude/agents/make-video.md` → `.claude/refs/make-video.md`. Renders ONE walkthrough/demo MP4 for a page/article from a data-driven spec, using the OFFICIAL Hyperframes skill (`npx skills add heygen-com/hyperframes`; installed to `.agents/skills/hyperframes-*` + workflows) as the authoritative guide. Reusable template lives in `.claude/make-video/` (`build-composition.mjs` generator + vendored GSAP + the site fonts as woff2 + the TreAxe example spec). Renders OFFLINE only (HARD): the MP4/poster are committed static assets under `public/`; `npm run build` only copies them, never renders.
- Proof shipped: rendered `public/treaxe/walkthrough.mp4` (1920x1080 H.264/yuv420p, 26.0s, 9.6MB, faststart) + `public/treaxe/walkthrough.poster.jpg` from the 6 real TreAxe screens (dashboard, leads/CRM Kanban, estimates, invoices, project-overview, proposal-PDF) with captions trimmed from `src/data/treaxeShots.ts` (no em-dashes). Validated via the documented Hyperframes loop: lint 0-errors + validate (WCAG AA, no console errors) + inspect 0-layout-issues. Embedded a base-aware `<video ... poster={asset(...)}>` in a new "Walkthrough" section of `src/pages/work/treaxe.astro` (existing gallery kept).
- Request→fulfill handoff (respects no-nested-spawn): added `state/video-queue.md`; writer + scanner refs/defs now REQUEST a video (queue row, flagged in their report) instead of rendering; `AGENTS.md` gained a "Fulfilling a video request" section (lead spawns `subagent_type: make-video`). Moved `make-demo-video` out of `roadmap.md` (planned → built as `make-video`).
- Env note: ffmpeg/ffprobe at ~/.local/bin; Hyperframes' Chrome downloader is offline-blocked, so HYPERFRAMES_BROWSER_PATH points at the installed Playwright chrome-headless-shell. `npm run build` PASS (render is NOT a build step). NOT deployed (owner reviews the video first).

## 2026-06-15 loop-run | wrote contract-retriever: Contract-Retriever (RAG→agentic rebuild) covered
- Grounded in the two real repos: `Contract-Retriever-RAG` (v1: Next.js + DeepSeek router + hybrid SQLite/RAG + the pure `validateAnswer()` gate) and `Contract-Retriever-Agentic` (v2: Python FastAPI + a Claude Agent SDK loop over a `knowledge/` tree of human-readable `data_structure.md` maps, no embeddings). Same product (cited grounded answers, honest refusal, no fabricated cross-domain join), swapped retrieval engine; the post centers on how the verification strategy had to change (deterministic gate → statistical bar: golden evals + trace inspection + repeated-N + LLM judge).
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds).
- Build: PASS. Em-dash / AI-tell grep: clean (no matches).

## 2026-06-15 loop-run | wrote econorag: EconoRAG (LangGraph ReAct agent over FRED) covered
- Grounded in the two real repos (cloned to `.scratch/econorag-src/`, gitignored): `my-langgraph-rag` backend (the actual `src/retrieval_graph/graph.py` ReAct loop with `MAX_TOOL_CALLS = 4`, `should_continue` router, agent↔tools nodes; `fred_tool.py`'s real FRED API calls (chart via official `fredgraph.png`, recent data + series notes, series→release schedule, release-by-name structure, catalog search); `fraser_tool.py`'s Postgres trigram FOMC-title search; `prompts.py`'s "must have a tool result before answering, no general world knowledge" RESPONSE_SYSTEM_PROMPT; `state.py`'s out-of-band `attachments`/`series_data` channels; `retrieval.py`'s `user_id` filter on the vector store; `api_server.py`'s FastAPI `/ask` + Supabase JWT; `fly.toml` Fly.io deploy) and `econorag-frontend` (Next.js chat client + the `POST /ask` contract README). Confirmed owner authorship via git (`qufeiz`); credited the base as an adaptation of LangChain's retrieval-agent-template (orig template author noted in pyproject). Framed as predecessor of the FredGPT line per `sources.md`; touched NO FredGPT confidential material.
- Used wired rendering: a mermaid agent↔tools loop diagram, two annotated python fences (`should_continue`, the `user_id` retrieval filter) + one text fence (the system-prompt rule), one `:::note` callout. Cross-linked to `/work/fredgpt` and `/notes/contract-retriever`.
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds). order: 6, category: projects, kind: concept.
- Build: PASS (exit 0; `/notes/econorag/index.html` rendered). Em-dash / ` -- ` / AI-tell grep: clean (no matches). No video requested.

## 2026-06-16 loop-run | wrote jobright-agent: jobright-agent (unsupervised Jobright application automation) covered
- Grounded in the REAL `jobright-agent` repo (in `~/Projects`, no clone needed): the `jobright-apply` SKILL.md, `references/apply-agent-runbook.md`, `references/ats-gotchas.md`, the canonical ATS library (`scripts/ats/dispatch.mjs` host-routing + walled-platform bailout, `scripts/ats/_lib.mjs` stealth launch + `typeInto` + `classify`, `ats/ashby.mjs` header), and the factual `state/applied.jsonl` outcome spread (19 lines: 5 confirmed / 5 failed / 1 submitted-no-email / skipped-walled + skipped-ineligible). Centered on: the never-auto-submit human gate, orchestrator + throwaway apply-subagent split (one app/run), two-channel verification + the honest status ladder, the ATS dispatcher (route by host, bail before fill on walled platforms), CAPTCHA-is-a-hard-stop, never-invent-personal-data, and the encoded gotchas (type-don't-inject React inputs, spam-detection stealth at submit, rotating verification-code one-shot handshake).
- PII/confidentiality: HELD. The source carries real name/phone/email/LinkedIn/address/employer names (skip-PII territory: `CV-qufeiz.pdf`, `_lib.mjs` PROFILE, gotchas). Article publishes MECHANISM only under "Qufeiii" — no real name, contact, third-party employer names, or specific company applied to. PII grep on the article: clean.
- Used wired rendering: a mermaid orchestrator→subagent→gate→verify→log flow, two `:::warning`/`:::note` callouts (the submit gate, the honest-status ladder), two annotated JS fences (`routeFor` host-router, `typeInto`). Cross-linked to `/notes/my-agent-teams`, `/notes/independent-verification`, `/notes/the-ratchet`. order: 8, category: projects, kind: concept.
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds).
- Build: PASS (exit 0; `/notes/jobright-agent/index.html` rendered). Em-dash / en-dash / ` -- ` / AI-tell grep: clean (no matches). No video requested.

## 2026-06-18 loop-run | wrote x-agent: x-agent (headless X automation + voice gate) covered
- Grounded in the REAL `x-agent` repo (in `~/Projects`, no clone needed): `AGENTS.md` (the cookie-seed session model + script catalog + the two gates + the auto-logged knowledge base), `.claude/skills/x-post/SKILL.md` + `auto-loop.md` (the original-post path: two content sources, the default-deny `--shareable` sweep, the layered privacy stack, the meaning-layer self-check, human approval), `.claude/skills/x-twitter-growth/SKILL.md` (the reply path), `references/voice.md` (the de-AI voice gate this writer mirrors), `scripts/x-post-playwright.mjs` (the headless post mechanism: seeded storageState, type+send, read-the-result-back, the retry/dual-view permalink capture, the idempotent-by-url `logPost`), and `scripts/tweet_composer.py` (the deterministic rules-as-data voice validator, exit 1 on AI tells incl. the em-dash + arrow-glyph rules). Posted log characterized QUALITATIVELY only (carries both original posts and replies, some portfolio-keyTake-seeded) — no fabricated counts.
- Honesty/confidentiality: HELD. Mechanism only. No account handle (the repo's `@ChristophZhang` never appears in the article), no credentials/cookies/token values, no seeded-cookie contents, no personal posts, no private life-wiki corpus. Collaborations N/A (solo project). Verified by grep: zero handle, zero raw-token-value leak, prose em-dash-free (the only `—`/`–`/` -- ` and blacklist words sit INSIDE quoted gate-rule code/examples — that IS the article's subject). Reflexively noted the voice gate is the SAME no-em-dash/no-AI-tells floor this portfolio writer mirrors.
- Used wired rendering: a text fence for the cookie-seed flow, an annotated python fence quoting the real `EM_DASH` rule, a mermaid draft→gate→send→readback→status→log flow, `:::warning` (auth is never content) + `:::tip` (the same floor both directions) callouts. Cross-linked to `/notes/my-agent-teams`, `/notes/orchestrate-gate-ratchet`, `/notes/jobright-agent`. order: 9, category: projects, kind: concept.
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds — the seam back to the consumer itself).
- Build: PASS (exit 0; `/notes/x-agent/index.html` rendered). No video requested. Not deployed (lead deploys).

## 2026-06-19 loop-run | wrote determinism-around-stochasticity: agent-systems concept (gating every stochastic step) covered
- Concept post (`category: agent-systems`, `kind: concept`, order 4) — second move of the orchestrate→gate→ratchet thesis, expanded. Thesis: an LLM call is a sample from a probability distribution, so you never trust a single run, you wrap it in a deterministic check that returns the same verdict on the same input. "The LLM proposes, the deterministic layer disposes."
- Grounded in REAL code + docs (no invented anything): the engineer's pure `validateX()` content-fidelity gate, pinned by a unit test that "passes every golden example and fails every toy example" (`~/.claude/agents/refs/engineer/feature-design/SKILL.md:599`); the verifier's Gate 1 removable-handler proof (break the handler → red) + the "couldn't verify X, never fake a pass" rule (`~/.claude/agents/verifier.md`); x-agent's `tweet_composer.py` rules-as-data voice gate (real `EM_DASH` regex block quoted, exit 1 on a tell, `--selftest` fixtures: known-bad must fail, golden must pass); x-agent's `privacy_scan.py` fail-closed scanner (three run points, DEFAULT-DENY `--shareable`, and the load-bearing exit-2-on-empty-protective-set rule — "empty means mis-pointed, not safe"). Tied the meta-miss (strengthen the check, not the instance) back to the ratchet.
- Used wired rendering: a mermaid sample→candidate→gate→world loop, a `:::warning{title="Empty does not mean safe"}` callout for the fail-closed exit-2 rule, three annotated code fences (`rfi-validation.ts`, the real `tweet_composer.py` EM_DASH block, none invented). Cross-linked to `/notes/orchestrate-gate-ratchet`, `/notes/independent-verification`, `/notes/the-ratchet`.
- Honesty gate: PASS. No invented metrics. Collaborations N/A (concept piece about the owner's own systems/gates). No FredGPT `Private/` material; no FredGPT code link. Every gate named maps to a real script/doc. Style: no-em-dash — the ONLY `—`/`–`/` -- ` in the file sit inside the quoted `EM_DASH` gate-rule code fence (that rule IS the article's subject); prose is em-dash-free. AI-tell grep: clean (the one alternation hit was a false positive; "journey" only appears as the technical term "journey test"). keyTakes: 5 standalone one-liners filled.
- Build: PASS (exit 0; `/notes/determinism-around-stochasticity/index.html` rendered, 15 pages). No video requested. Not deployed (lead deploys).

## 2026-06-21 loop-run | wrote startup-experiments: portfolio.zip › Startup (early-venture experiments, zoning-copilot lead) covered
- Project post (`category: projects`, `kind: concept`, order 10). Framing per the queue: "things I tried to start" — early-venture experiments, none shipped revenue, zoning-copilot the strongest. No invented metrics/users/outcomes; the other three scoped honestly (thin = said so).
- Source: extracted JUST `portfolio/Startup/*` from `~/Projects/portfolio.zip` to `.scratch/startup-src` (gitignored), read the real code, then `rm -rf` the extracted copy. Grounded in: zoning-copilot's actual Python — `main.py` (FastAPI `/query` + SSE `/query/stream`), `pipeline.py` (city router → browser session → root TOC → LLM drill → answer), `crawler.py` (Playwright launches headless Chromium, intercepts the `codesToc` request to read jobId/productId, then runs `fetch(..., credentials:'include')` INSIDE the authenticated page; `fetch_children`/`fetch_content` + UI-chrome filter), `navigator.py` (the recursive LLM navigator: pick 1-4 headings/level, depth cap 6, dedupe on first 200 chars; real PICK_SYSTEM prompt quoted), `answerer.py` (the strict "answer ONLY from provided sections, quote IDs exactly, say if ambiguous" prompt → structured `ZoningAnswer` with allowed/key_rules/citations; pipeline auto-appends fetched section IDs to citations, raises if nothing usable), `models.py`, `config.py` (16 CA cities by Municode URL), `requirements.txt` (fastapi/playwright/openai/numpy), `frontend/app/page.tsx` (Next.js + shadcn cards streaming the per-depth nav events). HONESTLY flagged that `planner.py`/`rag.py` (TOC pre-filter + section planner + subsection chunker + optional embeddings) exist but the live pipeline does NOT call them — built the indexed path, shipped the live-navigation one.
- Supporting sub-projects, scoped honestly (not inflated): AppraisalOS = a Technical Design Document (.docx) for a human-in-the-loop appraisal-ops system (SSOT data model + n8n workflow blueprint) — design, NOT running code; said so. n8n folder = screenshots only, mentioned not written up. wordpressseo = a REAL 29-node n8n workflow JSON (`PKPROGO工作流.json`: manualTrigger/RSS → 5× OpenAI translate+title+category+tags → Telegram approve/reject → WordPress draft, Google-Sheets URL dedupe) + a Chinese project-spec docx for a poker-news site; described from the actual node graph.
- PII/confidentiality: HELD. The zip also contains the owner's real-name CV/cover/enrollment/essay PII docs elsewhere — pulled NONE of it; stuck to the Startup project code/mechanism. Published under "Qufeiii". No Bosch/Tessera, no FredGPT material touched.
- Used wired rendering: a mermaid TOC-drill flow (short node labels), one `:::tip` (why run fetch from inside the page) + one `:::note` (the built-but-unshipped planner/rag path), three annotated fences (`crawler.py` request-intercept, the `navigator.py` pick prompt as text, the `answerer.py` CRITICAL RULES as text). Cross-linked to `/notes/orchestrate-gate-ratchet`. 6 keyTakes filled (x-agent post seeds).
- Honesty/style: PASS. Em-dash / ` -- ` grep: clean. AI-tell grep: clean (the "journey" alternation had zero hits in this article). Build: PASS (exit 0; `/notes/startup-experiments/index.html` rendered, 17 pages). No video requested. Not deployed (lead deploys). Cleaned up: `.scratch/startup-src` removed.

## 2026-06-23 loop-run | wrote smartwatch-user-research: portfolio.zip › SmartWatch用户调研 (CMU smart-fitness UX study) covered
- Project post (`category: projects`, `kind: concept`, order 11). Framed honestly as a CMU course project on smart fitness equipment, told in two halves: discovery + a build/usability test. No invented metrics; the test was direct observation, so detection accuracy is kept qualitative ("most of the time", "occasional misses") exactly as the source notes state it. Never implied a shipped commercial product.
- Source: extracted JUST `portfolio/SmartWatch用户调研/*` from `~/Projects/portfolio.zip` (ABSOLUTE path) to `.scratch/smartwatch` (gitignored), converted the three real `.docx` via python-docx (pandoc absent). Grounded in: `Interview.docx` (the preliminary secondary research on sedentary work with its real cited studies/links, the 5 prepared questions, and 10 spontaneous in-person interviews with the per-interview field notes + the owner's own "overall summary" of patterns) + `Test Plan.docx` (the smart-dumbbell test: research question, hypothesis, 8-participant think-aloud design, the 4 exercises (bicep/hammer/Zottman curl + lateral raise), the script, the method note about over-explaining early, and the 8 participant write-ups + conclusion) + `Assignment_ Smart Fitness Equipment.docx` (the CPX sensor-mount design, why the middle of the handle, the water-bottle simulation, the watch-vs-dumbbell sensor-orientation difference, and the real-product durability/charging/usability challenges).
- Findings carried into the article (true to source): people sit 6-10h/day and "push through" despite awareness; awareness rarely became a workplace change; a sharp age split in behavior; almost nobody wanted a tracker (they wanted built-in movement). Build/test: the hands-free no-buttons interaction held (the design goal) but classification of similar lifts (bicep vs hammer curl) failed, fast/sloppy reps dropped, a cold start on the first reps; the behavioral finding that participants silently slowed + cleaned up their form to be read (flagged as a real failure mode: a detector that fails legibly trains its user).
- PII/confidentiality: HELD. The owner's real name does NOT appear in these docs; site identity stays "Qufeiii". All third-party interviewee + participant real names and their employers (e.g. the gym/office-park strangers, the test friends/classmates) were ANONYMIZED to role + age band only — none surfaced (word-boundary grep on both the .md and the built HTML: clean). No Bosch/Tessera, no FredGPT material, no barred zip docs touched.
- Used wired rendering: one `:::note` (the research-assistant-for-sources disclosure + first-hand protocol), one `:::tip{title="Why the dumbbell, not the watch"}`, one `:::warning{title="A detector that fails legibly trains its user"}`; a bulleted exercise list; no mermaid/screenshots (the source ships no shareable product screens — the docx photos are of named participants, barred). Cross-link: none forced (no honest project-slug tag applies).
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds).
- Honesty/style: PASS. Em-dash (—) / en-dash (–) / ` -- ` grep: clean. AI-tell grep: clean. Build: PASS (exit 0; `/notes/smartwatch-user-research/index.html` rendered, 18 pages; callouts → `<aside>`, date `Jun 23, 2026`). No video requested. Not deployed (lead deploys).

## 2026-06-23 loop-adjust | portfolio-writer not spawnable as a subagent_type this fire -> use general-purpose pointed at the def+ref
- This cron fire, `Agent(subagent_type: portfolio-writer)` failed: "Agent type 'portfolio-writer' not found" — only the global/plugin agent types were registered in the session, not the repo's project-level `.claude/agents/*.md`. (Harness-dependent; project-level subagent types are not always exposed as spawnable types.)
- Robust fallback used (worked cleanly, produced an honest build-green article): spawn `subagent_type: general-purpose` and instruct it to ASSUME the writer persona by reading + following `.claude/agents/portfolio-writer.md` AND `.claude/refs/write-article.md` exactly (plus `state/sources.md` gates + `src/content/SCHEMA.md`). The agent def is just a persona pointing at the ref, so a general-purpose worker fed those two files does the identical job.
- Future fires: try `subagent_type: portfolio-writer` first; if it 404s, fall back to general-purpose + the def/ref pointer above. The lead's independent step-2 gate (honesty/PII/build) is unchanged regardless of which worker ran.

## 2026-06-25 loop-run | wrote context-engineering: agent-systems concept (bounded context, throwaway subagents) covered
- Concept post (`category: agent-systems`, `kind: concept`, order 5) — expands Move 1 of the orchestrate→gate→ratchet thesis into a full piece. Thesis: a bigger context window does NOT fix context rot; the fix is architectural — keep the plan in a small long-lived orchestrator context and run all grunt work in bounded throwaway subagents, so context never accumulates where the plan lives. Closes on "request, do not spawn" and the no-nested-spawn rule that keeps the agent hierarchy flat.
- Grounded in REAL systems (read the actual files, invented nothing): this repo's own `.claude/AGENTS.md` — quoted its "The role — a lead that holds the plan and does NO grunt work" paragraph verbatim in a `:::note`; the named worker subagents (`portfolio-writer`/`portfolio-refresh`/`make-video`) each pointed at one `.claude/refs/*.md` how-to (`.claude/agents/*.md` confirmed present); the "disposable subagent contexts so the lead's context stays lean and strategic" rationale; the request→fulfill handoff (worker writes a `state/video-queue.md` row / report flag, the lead spawns `make-video`) that exists BECAUSE a worker subagent cannot spawn another subagent. Fleet cross-references pulled from the already-published pieces: jobright's throwaway apply-subagent (one application/run), x-agent's lead-decides-timing-then-delegates-each-post, the build team's engineer fanning a 100+ button audit out to parallel subagents. Used the writer loop itself as the honest live example ("you are reading the output of that loop right now").
- Used wired rendering: a mermaid lead→worker→report flow with the heavy context dying in the discarded subagent; one `:::note` quoting the real orchestrator runbook; one `:::tip{title="Request, do not spawn"}` for the no-nested-spawn handoff. Cross-linked to `/notes/orchestrate-gate-ratchet`, `/notes/my-agent-teams`, `/notes/jobright-agent`, `/notes/x-agent`.
- Honesty gate: PASS. No invented metrics/benchmarks (the angle is mechanism-only; the only quantity is "one application per run", which is the real design). It is about the owner's OWN systems — no collaboration-credit or FredGPT-confidentiality surface touched. Every claim grounded in a file that exists (AGENTS.md, the agent defs, the two published fleet articles). Style: no-em-dash — the 4 `—` in the BUILT HTML are the site's global template chrome only (the `<title>` `… — Writing · Qufeiii` separator + the `aria-label="Qufeiii — home"` brand), identical on every article; the article SOURCE (body + deck + keyTakes) is em-dash/en-dash/arrow-glyph clean. AI-tell grep: clean (no blacklist words, no "journey", no "not just X, it's Y", no significance inflation).
- keyTakes: 5 standalone one-liners filled in frontmatter (x-agent post seeds).
- Build: PASS (exit 0; `/notes/context-engineering/index.html` rendered, 19 pages; mermaid block + 2 `<aside>` callouts present; cross-links based to `/portfolio/notes/...`; date `Jun 25, 2026`). No video requested. Not deployed (lead deploys).

## 2026-06-27 loop-run | wrote compiled-knowledge: agent-systems concept (life-wiki self-ingest, SYSTEM/mechanism only) covered
- Concept post (`category: agent-systems`, `kind: concept`, order 6). Angle: knowledge as a COMPILED, regenerable build output rather than a pile of transcripts. Years of raw chat logs get ingested into an interlinked, time-aware, deduplicated markdown wiki by a self-ingest loop; the wiki is a build output you can regenerate (source compiles to artifact), not a hand-maintained doc. Mechanism and architecture only.
- ⚠️ HARD CONFIDENTIALITY: the subject is the owner's PRIVATE life-wiki built from PERSONAL chat logs. Wrote about the SYSTEM ONLY. Referenced ZERO private corpus content — never opened `raw/`, `wiki/` pages, `index/`, `log.md`, `_cursor`, or `life-wiki-vault.tar.gz`. Grounded EXCLUSIVELY in the system/pipeline layer: `README.md` + `CLAUDE.md` (the maintainer schema: page types, the keep/skip triage gate, the canonical-claim dedup resolve-before-write protocol, content-vs-export dating + `date_confidence`, append-dated-stance-log-on-contradiction, sensitivity = PUBLISH gate not internal censorship, the delegate→review→self-correct loop, schema-evolution-is-human-gated), `tools/extract_chatgpt.py` (deterministic no-LLM extractor + the priority heuristic — quoted the real `priority()` function), `tools/regen.py` (the derived-views build step — quoted the real timeline-match pass), `tools/queue.py` (resumable queue primitives), `.claude/commands/ingest-batch.md` + `ask.md` (the worker + read-only reader). Verified by grep: no `raw/chatgpt`/`wiki/eras`/`conv-<hash>`/vault/`index/`/`_cursor` references in the article — only `tools/*.py` + `.ingest/queue.jsonl` (system filenames) appear.
- Karpathy's LLM-wiki idea (`karpathy-llm-wiki` = `skip-not-mine`): CREDITED as the inspiration (linked the public gist as external), explicitly NOT claimed as the owner's doc; the owner's IMPLEMENTATION (the self-ingest loop) is what the piece is about, per a `:::note{title="Mechanism only"}` that states the corpus is private and the system is describable without it.
- Used wired rendering: a mermaid raw→queue→subagent→pages→regen→review flow (heavy transcripts die with the throwaway subagent), one `:::note` (mechanism-only confidentiality boundary), two annotated python fences quoting REAL code (`regen.py` timeline pass, `extract_chatgpt.py` `priority()`). Cross-linked to `/notes/context-engineering` (the throwaway-subagent/bounded-context overlap) + `/notes/my-agent-teams` (fleet hub).
- Honesty gate: PASS. No invented metrics (no fabricated corpus size, entry counts, or accuracy numbers — none stated). Collaborations N/A (the owner's OWN system; inspiration credited to Karpathy, implementation is the owner's). No FredGPT/Bosch/PII surface touched. Every claim maps to a real system file. Style: source is em-dash/en-dash/` -- `/arrow-glyph clean (grep clean); the only `—` in the BUILT HTML is the site's global template chrome (the `<title>` separator + `aria-label="Qufeiii — home"`), identical on every page. AI-tell grep: clean.
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds).
- Build: PASS (exit 0; `/notes/compiled-knowledge/index.html` rendered, 20 pages; mermaid + `<aside>` callout present; cross-links based to `/portfolio/notes/...`; Karpathy gist external link passes through; date `Jun 27, 2026`). No video requested. Not deployed (lead deploys).

## 2026-06-29 loop-run | wrote honest-automation: agent-systems concept (fail-closed gates, never-claim-an-effect-you-can't-see, human-approval floor) covered
- Concept post (`category: agent-systems`, `kind: concept`, order 7). Angle: agents that ship real, UNSUPERVISED work without overclaiming. Thesis: autonomy is safe only when the agent is built to FAIL CLOSED and to report precisely what it did and did NOT verify. Four mechanisms: fail-closed/default-deny gates, "never claim an effect you can't see" (read the result back, log an honest status), confidentiality/never-invent holds, and the human-approval floor for irreversible actions.
- Grounded in REAL code (read the actual files, invented nothing): x-agent `.claude/skills/x-post/scripts/privacy_scan.py` — quoted the real fail-closed assertion (`if not names and wiki_root_is_nonempty(wiki_root): sys.exit(2)`), the `--shareable` DEFAULT-DENY candidate spine ("merely not blocked" != shareable), and the exit-code contract (0 clean / 1 blocked / 2 config error); x-agent `.claude/skills/x-twitter-growth/scripts/tweet_composer.py` — validator-not-generator, exit 1 on AI tells, `--selftest` gates the gate; x-agent `.claude/skills/x-twitter-growth/scripts/x-post-playwright.mjs` — quoted the real `done(posted ? 'POSTED' : 'UNCONFIRMED', ...)` line, `POST STATUS: POSTED | UNCONFIRMED | ERROR` driven by the read-back, idempotent by url in `posted.jsonl`; this portfolio's `.claude/AGENTS.md` — the honesty floor (skip rather than filler), the lead's INDEPENDENT step-2 gate before publish (worker isn't its own final gate), and deploy-verify ("never claim a deploy you can't see" — curl-retry the live Pages URL); jobright-agent (from the published `/notes/jobright-agent`) — never-auto-submit human gate, two-channel honest-status ladder (confirmed / submitted-no-email / unverified / failed), CAPTCHA-is-a-hard-stop + walled-platform bailout, never-invent-personal-data.
- Used wired rendering: a `:::warning{title="Default-deny, not blocklist"}`, a `:::note` for the trusted-to-do-not-to-decide framing, one annotated python fence (the real fail-closed assertion), one annotated js fence (the real status line), and a mermaid act→read-back→honest-status flow. Cross-linked to `/notes/determinism-around-stochasticity` (the upstream "gate the sample" idea), `/notes/independent-verification` (the checker-is-never-the-author rule), `/notes/x-agent`, `/notes/jobright-agent`, `/notes/my-agent-teams`, plus in-prose `/notes/context-engineering` + `/notes/compiled-knowledge`.
- Honesty gate: PASS. No invented metrics/counts (mechanism-only; no fabricated post/application counts — the jobright "full spread of statuses" is qualitative, as in the source article). These are the owner's OWN systems — no collaboration-credit surface and no FredGPT/Bosch-confidentiality surface touched. No credentials/cookies/account-handle/TreAxe-secrets/Barrat/PII exposed. Every cited behavior verified by reading the real file (privacy_scan.py, tweet_composer.py, x-post-playwright.mjs, AGENTS.md). Style: SOURCE body + deck + keyTakes are em-dash/en-dash/` -- `/arrow-glyph clean (grep clean) and AI-tell-grep clean (no blacklist vocab, no "journey", no "not just X, it's Y", no significance inflation); the only `—` in the BUILT HTML is the site's global template chrome (the `<title>` separator + `aria-label="Qufeiii — home"`), identical on every page.
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds).
- Build: PASS (exit 0; `/notes/honest-automation/index.html` rendered, 21 pages; mermaid + 2 `<aside>` callouts + 2 annotated code fences present; all 7 cross-links based to `/portfolio/notes/...`, zero bare `/notes/`; date `Jun 29, 2026`). No video requested. Not deployed (lead deploys).

## 2026-07-01 loop-run | wrote packaging-an-agent: agent-systems concept (repo-level capability model vs. a standing team) covered
- Concept post (`category: agent-systems`, `kind: concept`, order 8). Angle: the SAME orchestrate-then-delegate spine ships in two PACKAGINGS, and the choice is about the shape of the work, not taste. Shape A = a repo-level capability model (project-level subagents that live in one codebase); Shape B = a standing PM/engineer/verifier team convened for open-ended build work. Names the tradeoffs honestly.
- Grounded in REAL files (read them; invented nothing): Shape A from THIS repo — `.claude/AGENTS.md` ("Capabilities are PROJECT-LEVEL SUBAGENTS": the def→ref pointer split, the lead that DISCOVERS agents at runtime via `ls .claude/agents/*.md` and spawns by name with a one-line goal, "How to add a capability / the capability ratchet") + the THREE real agent defs each a persona + a "Read and follow `.claude/refs/<name>.md` exactly" pointer (`portfolio-writer`→`write-article.md`, `portfolio-refresh`→`refresh-portfolio.md`, `make-video`→`make-video.md`), verified by `ls .claude/agents/ .claude/refs/`; quoted the real `portfolio-writer.md` body ("Read and follow ... exactly. That ref is your full how-to."). Shape B from `~/.claude/agents/{pm,engineer,verifier}.md` — the role split, the PM's design-approval gate (Phase 0, human-approved before any code, "a prior 'do it' never carries over"), the verifier that is "read-only on source: it did not write the code it checks" and owns the data-driven coverage design.
- Used wired rendering: THREE mermaid diagrams (the lead→def→ref fan-out for the 3 capabilities; a work→shape router; used `-->` mermaid edge syntax, same convention as the shipped articles) + one `:::tip{title="The property that makes shape A worth it"}` (versioned-with-the-code) + one annotated `md title=".claude/agents/portfolio-writer.md"` fence quoting the real def body. Cross-linked to `/notes/my-agent-teams` (the hub, both shapes), `/notes/context-engineering` (both are bounded-context orchestration; the request-do-not-spawn handoff), `/notes/orchestrate-gate-ratchet` (the shared spine), `/notes/independent-verification` (why shape B's independent grader is load-bearing). EXTENDS rather than duplicates: this piece is about the CONTAINER; the spine pieces are linked, not restated.
- Honesty gate: PASS. No invented metrics (mechanism-only; the only quantity is "three capabilities", which is the real count of `.claude/agents/*.md`). The owner's OWN systems — no collaboration-credit surface and no FredGPT/Bosch-confidentiality surface touched. No credentials/secrets/Barrat/TreAxe-demo-secrets/PII. Every cited file/behavior verified by reading it (AGENTS.md, the 3 portfolio agent defs + their refs' pointers, pm.md, verifier.md). Style: SOURCE body + deck + keyTakes are em-dash/en-dash/` -- `/arrow-glyph clean (grep clean, the only `-->` matches are Mermaid edges inside fenced blocks) and AI-tell-grep clean (no blacklist vocab, no "journey", no "not just X, it's Y", no significance inflation, no parallel triplets); the only `—` in the BUILT HTML is the site's global template chrome (the `<title>` separator + `aria-label="Qufeiii — home"`), identical on every page.
- keyTakes: 5 standalone one-liners filled in frontmatter (x-agent post seeds).
- Build: PASS (exit 0; `/notes/packaging-an-agent/index.html` rendered, 22 pages; 2 mermaid blocks + 1 `<aside>` tip callout + 1 annotated code fence present; all 4 cross-links based to `/portfolio/notes/...`, zero bare `/notes/`; date `Jul 1, 2026`). No video requested. Not deployed (lead deploys).

## 2026-07-04 loop-run | wrote nucleus-multi-tenant-isolation: Nucleus multi-tenant document isolation covered — ⚠️ CLIENT-WORK DRAFT (built + logged, NOT deployed)
- Project post (`category: projects`, `kind: concept`, order 12). FIRST Nucleus mechanism deep-dive; this is ACTIVE CLIENT WORK so it ships in DRAFT MODE per `sources.md` › Nucleus gate: write + build + ledger, NO deploy — the lead does a hard confidentiality review before it can go live.
- Angle: how a multi-tenant RAG assistant isolates each user's uploaded documents at the DATA layer (not the prompt), so no user can see/retrieve/overwrite another's file and a filename in one chat can't reach another chat's file. Three grounded layers: (1) owner-scoped storage path `<owner_id>/<sha256(docId)>` + the fail-closed delete assertion (`path.startsWith('${ownerId}/')` or refuse); (2) scope-qualified doc ids — `docIdFromFilename(name, scope)` folds 12 hex of `SHA-256(scope)` into the id so `report.pdf` in chat A vs chat B are DISTINCT documents (closes the cross-chat filename steal, bug F4) while same-scope re-upload still replaces; (3) the per-owner `withManifestLock` settle-safe promise chain serializing the `_files.json` read-modify-write so concurrent upload/upload + upload/delete don't drop an entry (bug F5), with the in-process-only limitation + the durable Postgres-table fix stated honestly.
- Grounded EXCLUSIVELY in `~/Projects/nucleus` branch `main`, commit `cb33230` mechanism code: read `src/lib/engine/doc-files.ts` (storagePath, storeKeyFromDocId, withManifestLock, updateFilesManifest, removeOriginalFile safety assertion, fetchOriginalFile owner-scoping), `src/lib/engine/ingest.ts` (docIdFromFilename scope suffix), `src/app/api/ingest/route.ts` (ownerId from session, chatId/spaceId required-scope guard), `src/lib/engine/file-scope.ts` (strict per-chat filter), and the RED-first tests `tests/api/doc-files-collision.test.mts` (F4 two-chat collision + same-scope replace control) + `tests/api/doc-files-race.test.mts` (F5 Promise.all store/store, store/remove, 3-way). Every claim traces to that code.
- ⚠️ CONFIDENTIALITY (the #1 gate on this piece): MECHANISM/CODE ONLY, `main` branch ONLY, client NEVER named. Did NOT read commit `82d31f4` (client bring-up runbook — contains `CLIENT-AGENT-RUNBOOK.md` + `message-to-jenny*`); did NOT open `clientchat.md`, any `message-to-jenny*` / `client-messages-draft`, or the client-rebrand branches (`client`/`client-sync`/`ui-redesign`). Referenced ZERO: client identity/business/industry, contact name ("Jenny"), API keys / Supabase service-role / demo logins / secrets, client dollar figures / budgets / credit balances, PRD/golden-bar specifics. Verified by grep on BOTH the source `.md` and the built HTML: zero hits for jenny/clientchat/upwork/deepseek/ollama/service-role. The public framing matches the site ("a real production multi-tenant AI app for a private client, client not named").
- Honesty gate: PASS. No invented metrics (mechanism-only; the only numbers — "12 hex chars", "SHA-256" — are the real code constants). The owner did ALL the engineering (no collaboration-credit surface). Every substantive claim maps to a file read. Style: SOURCE body + deck + keyTakes are em-dash/en-dash/` -- `/arrow-glyph clean (grep clean; the mermaid `-->` edges sit inside the fenced block and are not prose) and AI-tell-grep clean (no blacklist vocab, no "journey", no "not just X, it's Y", no significance inflation, no parallel triplets). Wired rendering: 1 mermaid (per-owner lock serialization), 2 `<aside>` callouts (`:::note` why-not-the-prompt, `:::warning` what-the-lock-does-not-close), 5 annotated `ts` fences quoting real `doc-files.ts`/`ingest.ts`/`route.ts` code. Cross-linked to `/notes/honest-automation` (the name-what-you-didn't-verify discipline). date `Jul 4, 2026`.
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds).
- Build: PASS (exit 0; `/notes/nucleus-multi-tenant-isolation/index.html` rendered, 23 pages; 1 mermaid block + 2 `<aside>` callouts present; cross-link based to `/portfolio/notes/honest-automation`, zero bare `/notes/`). No video requested. ⛔ NOT DEPLOYED — CLIENT-WORK DRAFT awaiting the lead's confidentiality review.

## 2026-07-05 loop-run | HELD (no new article) — first Nucleus client-work draft awaits owner confidentiality sign-off
- Re-armed (STEP 0, self-renew). Dedup PROCEED. But every writeable queue item is a Nucleus (client-work) deep-dive, and `nucleus-multi-tenant-isolation` is still an un-reviewed DRAFT pending the owner's confidentiality OK. Chose NOT to stack a second un-reviewed client draft ahead of the review gate, and NOT to pivot to an un-steered TreAxe/FredGPT track. Honest hold, not filler. Loop keeps re-arming; resumes (drafting the next Nucleus piece, draft-or-auto per owner's decision) once the owner signs off on the first draft.

## 2026-07-07 loop-run | wrote nucleus-byo-keys: Nucleus per-user BYO API keys + house-key allowlist + zero-token test covered — ⚠️ CLIENT-WORK DRAFT (built + logged, NOT deployed)
- Project post (`category: projects`, `kind: concept`, order 13). SECOND Nucleus mechanism deep-dive. Explicitly directed by the lead (top of the Nucleus queue), so it ships alongside the first draft in the same owner confidentiality-review batch (DRAFT MODE per `sources.md` › Nucleus gate: write + build + ledger, NO deploy).
- Angle: a multi-tenant AI app where each user brings their OWN model key instead of silently riding one shared key. Three grounded mechanisms: (1) ONE credential resolver shared by both answer engines (`anthropic-auth.ts` `resolveAnthropicAuth`: own -> house -> none, each settings read wrapped in `.catch(()=>"")` so it fails CLOSED — a DB hiccup degrades to the honest no-key result, never fails OPEN onto the shared key), returning the answer-shaped `noKeyAnswer` (grounded=false, ZERO upstream calls) for an unflagged keyless owner, plus the agentic engine's per-request `ownKeyEnv` SDK-subprocess env injection (fresh object per call, never mutates the shared process.env, concurrency-safe); (2) the shared "house" key as an admin-only ALLOWLIST — a per-user `house_key_enabled="1"` flag written SOLELY from the admin Users route (`setHouseKey`, admin-guarded 403-for-users, not self-grantable; the per-user settings route deliberately refuses to touch it), grandfathered additively by migration 018 (`insert ... select from auth.users ... on conflict do nothing` — never overwrites a key or a later-toggled flag, new users default OFF); (3) write-only key storage (server derives + keeps only name/last4/created, `getSettings` returns `anthropic_api_key_set` bool + metadata, never the value; blank submit = leave key alone, `__clear__` sentinel = full delete) + a ZERO-TOKEN "Test connection" that validates a pasted key with `client.models.list()` (an authenticated GET that bills nothing, NEVER `messages.create`) via the SAME resolver, with a `sanitizeError` redactor stripping bearer/api-key/long-token runs + no-500-on-bad-key.
- Grounded EXCLUSIVELY in `~/Projects/nucleus` branch `main`, commits `c1ad91b` ("per-user Anthropic keys + admin house-key allowlist") + `87e488b` ("zero-token Test connection + browser-verified key journey"): read `src/lib/engine/anthropic-auth.ts` (resolveAnthropicAuth, noKeyAnswer, resolveAnthropicClient, ownKeyEnv), `settings.ts` (the write-only key SettingKeys + `AdminSettings` masking + `listHouseKeyOwnerIds`), `answer-agentic.ts` (the c1ad91b env-injection diff), `answer-messages.ts` (the shared `makeClient = resolveAnthropicClient`), `src/app/api/settings/route.ts` (the write-only save/rename/clear semantics + house_key_enabled deliberately NOT handled here), `api/admin/users/route.ts` (the `setHouseKey` admin-only toggle), `api/test-connection/route.ts` (`testAnthropicKey` -> `models.list()` + `sanitizeError` + friendly no-500), and `supabase/migrations/018_house_key_allowlist.sql` (the additive grandfathering insert). Every claim traces to that code.
- ⚠️ CONFIDENTIALITY (the #1 gate): MECHANISM/CODE ONLY, `main` branch ONLY, client NEVER named. Did NOT read commit `82d31f4` (client bring-up runbook + `message-to-jenny*` + `CLIENT-AGENT-RUNBOOK` + `clientchat`); did NOT open the client-rebrand branches (`client`/`client-sync`/`ui-redesign`). Referenced ZERO: client identity/business/industry, contact name, ANY real API key / house-key value / key prefix, Supabase secrets, the ACTUAL allowlist contents (described THAT an admin-only flag gates the house key + HOW, never WHO), client dollar figures/credit balances/usage counts. Deliberately EXCLUDED the barred details that live in the commit MESSAGES (the "70 current users", the "client grandfathered onto the house key" line, the em-dash) — described grandfathering generically as "every account that existed at that moment." Verified by grep on BOTH source `.md` and built HTML: zero hits for jenny/clientchat/upwork/deepseek/ollama/service-role/70-current/grandfathered-onto-the-house/NUCLEUS 770.
- Honesty gate: PASS. No invented metrics (mechanism-only; the only literals — "last four", "models.list()", "018" — are real code). The owner did ALL the engineering (no collaboration-credit surface). Every substantive claim maps to a file read. Style: SOURCE body + deck + keyTakes are em-dash/en-dash/` -- `/arrow-glyph clean (verified by a python fence-stripping pass over the prose; the only `--`/`-->` hits sit in frontmatter fences + the mermaid block, and the two `-` inside quoted code strings are the real reworded code comments). AI-tell grep: clean (no blacklist vocab, no "journey", no "not just X, it's Y", no significance inflation, no parallel triplets). Wired rendering: 1 mermaid (credential-resolution flow), 2 `<aside>` callouts (`:::note` additive grandfathering, `:::tip` why-models.list-not-a-one-word-message), 6 annotated `ts` fences quoting real code. Cross-linked to `/notes/honest-automation` (the fail-closed / verify-what-you-can discipline). date `Jul 7, 2026`.
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds).
- Build: PASS (exit 0; `/notes/nucleus-byo-keys/index.html` rendered, 24 pages; 1 mermaid block + 2 `<aside>` callouts + 6 code fences present; cross-link based to `/portfolio/notes/honest-automation`, zero bare `/notes/`; Key takes block + date rendered). No video requested. ⛔ NOT DEPLOYED — CLIENT-WORK DRAFT joining the owner's confidentiality-review batch (first Nucleus draft still un-signed-off).

## 2026-07-09 loop-adjust | DRAFT-LEAK: "held" Nucleus client drafts were actually LIVE — fixed + ratcheted
- Root cause: "draft mode" was implemented as "write + build + DON'T run deploy." But the two Nucleus client drafts had NO `draft:true` frontmatter, and `publishedArticles()` only filters `!data.draft`. So when the 2026-07-07 Nucleus case-page-refresh deploy ran a FULL `npm run build && deploy.sh`, it rebuilt + published BOTH flagless drafts. They were live (HTTP 200, in /writing) for ~2 days. (Confidentiality-clean — both had passed the hard review — but the "held" claim was false.)
- Fix: added `draft: true` to both `nucleus-multi-tenant-isolation.md` + `nucleus-byo-keys.md`, rebuilt (pages excluded), redeployed → both now 404 / off /writing (verified). Held for real.
- Ratchet: `loop-prompt.txt` STEP 2 now defines CLIENT-WORK DRAFT MODE as "MUST set `draft:true`" (the build-excluding flag), with an explicit warning that skip-deploy alone leaks on the next full-site deploy. Future client-work articles set the flag at write time.
- This fire wrote NO new article (review batch already at 2; priority was the leak fix). Held.

## 2026-07-11 loop-run | HELD — 2 Nucleus client drafts (draft:true, offline) still await owner sign-off; no new article, no 3rd draft stacked.

## 2026-07-27 loop-run | wrote nucleus-local-mode: Nucleus local/Ollama model mode (data stays on the user's machine) covered — NORMAL publish (owner approved the Nucleus confidentiality bar 2026-07-24; built + logged, lead sweeps + deploys)
- Project post (`category: projects`, `kind: concept`, order 14). THIRD Nucleus mechanism deep-dive, top of the Nucleus queue, explicitly directed by the lead. Per the 2026-07-24 change the owner APPROVED the Nucleus confidentiality bar, so this ships as NORMAL publish (`draft: false`, written clean like an own-work article) — NOT the old client-work DRAFT mode. The lead does the hard confidentiality sweep + deploys; the writer does not deploy.
- Angle: a privacy posture for a multi-tenant AI assistant. One per-owner `model_mode` setting reroutes the whole answer path so chat round-trips through the user's OWN local model (Ollama) instead of a hosted API, so documents/questions never leave the machine — and it's ONE codebase, not a fork. Why it matters: regulated/sensitive data.
- Five grounded mechanisms: (1) the switch is a per-owner `model_mode` resolved at request time by `getModelMode`, which FAILS SAFE — anything not the literal `local`/`hipaa` resolves to `cloud`, so a blank/corrupt value can't half-route to an unwired backend; (2) ONE dispatch point in `/api/ask` — `local` runs `answerLocal` IN-PROCESS and is NEVER handed to `forwardToFlyAgent` (so a Local question physically can't take the cloud route), while the `cloud` case is byte-for-byte the prior behavior (one product, not two that drift), and `hipaa` returns an honest 503 rather than silently downgrading PHI-intent traffic to the non-BAA model; (3) `answerLocal` round-trips the question through the owner's OWN OpenAI-compatible endpoint (Ollama; `local_endpoint`/`local_model` per-owner settings) — the only outbound `fetch` is to the user's own address, no hosted provider, placeholder `Bearer ollama` (no billed key, Ollama ignores it); (4) honest v0.5 scope — chat-only, `grounded:false`, empty evidence, `model:local:<name>` attribution so a caller can PROVE the local path ran, plus a PROMPT-LEVEL honesty guardrail (`buildLocalSystemPrompt`) that NAMES the in-scope files but instructs the model to say it can't read them and FORBIDS inventing their contents (the GPU-day file-in-context upgrade is designed-not-built and the code doesn't pretend otherwise); (5) never-500 — unset/unreachable endpoint returns a friendly answer-shaped `localGuidance` result that names the address and fails in seconds (a dead host refuses immediately; a slow tunneled box is bounded by a timeout well under the route budget), plus a `$0` cost-block overwrite because the shared inspector prices at hosted rates which is wrong for a call on the owner's own hardware.
- Verification anchored to the REAL journey `tests/journeys/local-mode.mjs`: real browser + box Ollama (llama3.2:3b), ZERO Claude/Anthropic spend — selects Local + Saves through the real Settings UI, Test connection against the live endpoint, a genuine chat round-trip asserting the rendered reply + response JSON `model` starts with `local:`, then a "my uploaded documents" ask asserting `grounded===false` + zero `citation-chip` + an honest can't-read-docs reply, then restores mode=cloud through the UI (residue-clean).
- Grounded EXCLUSIVELY in `~/Projects/nucleus` branch `main`, commit `0c19d58` ("local mode v0.5: model_mode drives the engine"): read `src/lib/engine/answer-local.ts` (answerLocal, buildLocalSystemPrompt, unreachableResult, the $0 cost overwrite), `src/lib/engine/settings.ts` (`getModelMode` fail-safe + `getModelConfig` + the model_mode/local_endpoint/local_model SettingKeys + DEFAULTS), `src/app/api/ask/route.ts` (the lane dispatch + the hipaa 503 + local-never-forwarded-to-Fly), `src/components/models-panel.tsx` (the 3-way Cloud/HIPAA/Local switch + local endpoint/model fields + detect-models + `data-hydrated`), `docs/LOCAL-MODEL.md` (v0.5 scope, self-host vs tunnel, the GPU-day upgrade path), `tests/journeys/local-mode.mjs` (the live-verified assertions). Every claim traces to that code/doc.
- ⚠️ CONFIDENTIALITY (the #1 gate): MECHANISM/CODE ONLY, `main` branch ONLY, client NEVER named ("a real production multi-tenant AI app for a private client, client not named"). Did NOT read commit `82d31f4` (client bring-up runbook + `message-to-jenny*` + `CLIENT-AGENT-RUNBOOK` + `clientchat`); did NOT open the client-rebrand branches. Referenced ZERO: client identity/business/industry, the contact name, ANY real endpoint / hostname / IP / tunnel URL / API key / Supabase secret / demo login that could identify a deployment, client dollar figures / user counts / credit balances, PRD/golden-bar specifics. The `http://localhost:11434/v1` in the piece is the generic public Ollama default (from the model's own docs + placeholder UI), not a client deployment address. Verified by grep on BOTH source `.md` and built HTML: zero hits for jenny/clientchat/upwork/message-to/82d31f4.
- Honesty gate: PASS. No invented metrics (mechanism-only; literals like `qwen2.5`, `local:`, `grounded:false`, `Bearer ollama` are real code). The owner did ALL the engineering (no collaboration-credit surface). Every substantive claim maps to a file read. Style: SOURCE deck + prose + keyTakes are em-dash/en-dash/arrow-glyph CLEAN; the single `—`/`→` in the file sits inside a VERBATIM real-code quote (the `unreachableResult` answer string, confirmed verbatim against `answer-local.ts:237`), which the persona explicitly allows as the one exception. AI-tell grep: clean (no blacklist vocab, no "journey/landscape/realm", no "not just X, it's Y", no significance inflation, no parallel triplets). Wired rendering: 1 mermaid (model_mode lane dispatch), 3 `<aside>` callouts (`:::note` local≠fully-offline, `:::tip` why-grounded-false-is-honest, plus in-flow honest framing), 6 annotated `ts` fences quoting real code. Cross-linked to `/notes/nucleus-byo-keys` + `/notes/nucleus-multi-tenant-isolation` (the keys-and-privacy pair) + `/notes/honest-automation`. date `Jul 27, 2026`.
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds).
- Build: PASS (exit 0; `/notes/nucleus-local-mode/index.html` rendered, 25 pages; cross-links based to `/portfolio/notes/...` with zero bare `/notes/`; Key takes block + date rendered). No video requested. NOT DEPLOYED — the lead does the hard confidentiality sweep + deploy.

## 2026-08-03 loop-run | wrote nucleus-scale-to-zero: Nucleus Fly scale-to-zero + model-aware cost/usage metering covered — NORMAL publish (owner approved the Nucleus confidentiality bar; built + logged, lead sweeps + deploys)
- Project post (`category: projects`, `kind: concept`, order 15). FOURTH Nucleus mechanism deep-dive, top of the Nucleus queue, explicitly directed by the lead. Ships as NORMAL publish (`draft: false`, written clean like an own-work article) per the owner-approved Nucleus bar — NOT the old client-work DRAFT mode. The lead does the hard confidentiality + no-dollar-figures sweep + deploys; the writer does not deploy.
- Angle: cost engineering for a low-traffic production AI app. How to make a demo/low-traffic AI app cost near-nothing at rest WITHOUT a cold start that feels broken. Two mechanisms: (1) Fly suspend-when-idle / resume-on-request so the app runs (and bills) only while someone is actually using it; (2) model-aware cost/usage metering — measure real token usage per call, price it per model, append USAGE ledger rows, so cost is measured not guessed.
- Two grounded mechanisms: (1) Fly scale-to-zero — `fly.toml` `auto_stop_machines="suspend"` + `auto_start_machines=true` + `min_machines_running=0`: off by default, auto-resumes on the next request, bills only while serving; `suspend` (memory-snapshot resume) chosen over `stop` (full cold boot) so the first ask after a long idle reads as a small delay not a broken demo (the app runs the Next.js standalone server + an agent subprocess, exactly where keeping the initialized image around earns its keep); concurrency limits stay configured so a real burst still fans out — "zero machines when nobody's here, however many are needed when they are." (2) Model-aware cost/usage metering — the inspector cost line was hardcoded to one model's stale rates and labeled every call the cheap model, under-reporting the expensive one several times over and corrupting any ledger built on it; the fix prices each call by the RESOLVED model via a per-model price table (`answer-agentic.ts`), local models price at zero, and every answer engine (messages/agentic/local) feeds its resolved model in; the `buildCost` path (`answer.ts`) is honest-by-construction — cost=null-with-a-reason (never a guessed figure) when a provider doesn't report tokens or a model's price isn't tracked; and the end-to-end lifecycle journey (`tests/journeys/lifecycle.mjs` `logUsage`) appends one measured USAGE row per SDK call (resolved model + real reported in/out token counts + computed cost), defensively wrapped so a logging failure never reddens the run — cost is measured, not estimated.
- Grounded EXCLUSIVELY in `~/Projects/nucleus` branch `main`, commits `07a2f59` (`fly.toml` scale-to-zero) + `4f5234d` (model-aware pricing + USAGE ledger): read `fly.toml` (the http_service auto_stop/auto_start/min_machines block), `src/lib/engine/answer-agentic.ts` (the model-aware PRICES_PER_1M pricing + resolved-model plumb-through), `src/lib/engine/answer.ts` (`buildCost` honest-by-construction null-with-reason), the 1-line resolved-model additions in `answer-local.ts` + `answer-messages.ts`, and `tests/journeys/lifecycle.mjs` (`logUsage` USAGE-row append at each ask). Every claim traces to that code.
- ⚠️ CONFIDENTIALITY (the #1 gate, sharpest here because it's a COST article): MECHANISM/CODE ONLY, `main` branch ONLY, client NEVER named ("a real production multi-tenant AI app for a private client, client not named"). ZERO dollar figures / token counts / usage totals / credit balances / per-month spend / per-model rate numbers — the code excerpts are SANITIZED (the real PRICES_PER_1M rate numbers replaced with placeholders `PRICES_PER_1M`/`ZERO`/`FALLBACK`/`DEFAULT_MODEL`; the real absolute call-log path in `logUsage` genericized to `appendUsageRow`; the `pricingNote` strings paraphrased). The commit MESSAGES themselves carry dollar figures ($0.80/$4, $1/$5, $3/$15, ~$10-15/mo) AND the client contact name ("Chris") AND a "cumulative measured spend vs undisclosed top-up" line — ALL deliberately EXCLUDED from the article. Did NOT read commit `82d31f4` (client runbook). Did NOT read `docs/claude-call-log.md` for publishable content (it holds billed spend + the contact name) — only saw it named in the commit stat. Did NOT open client-rebrand branches. Referenced ZERO: client identity/business/industry (no hipaa/health/Azure reintroduced), the contact name, the Fly app name (`nucleus-agent`) / region (`iad`) / any hostname / deploy URL / key / Supabase secret. Verified by grep on the source `.md`: zero hits for chris/jenny/nucleus-agent/iad/dollar-figures/token-counts/haiku-4-5/sonnet-4-6/opus-4-5/hipaa/azure/health.
- Honesty gate: PASS. No invented metrics (mechanism-only; the ONLY numeric literals in the piece are the generic `1_000_000` per-1M-token divisor and the config value `min_machines_running = 0`, both structural, neither a cost/usage figure). The owner did ALL the engineering (no collaboration-credit surface). Every substantive claim maps to a file read. Style: SOURCE deck + prose + keyTakes + ALL code excerpts are em-dash/en-dash/arrow-glyph CLEAN — I cleaned the 4 glyphs that were in the sanitized code comments/strings (no verbatim-quote exception needed this time). AI-tell grep: clean (only hit was "journeys" in the real file path `tests/journeys/lifecycle.mjs`, not a tell). Wired rendering: 1 mermaid (idle→wake→serve→idle billing cycle), 2 `<aside>` callouts (`:::note` why suspend-not-stop, `:::tip` an honest cost line refuses to guess), 3 annotated code fences (`toml` fly config, `ts` model-aware pricing, `ts` honest buildCost, `js` logUsage). Cross-linked to `/notes/honest-automation` + `/notes/nucleus-byo-keys` + `/notes/nucleus-local-mode` (the cost-and-keys/privacy siblings). date `Aug 3, 2026`.
- keyTakes: 6 standalone one-liners filled in frontmatter (x-agent post seeds), all number-free.
- Build: PASS (exit 0; `/notes/nucleus-scale-to-zero/index.html` rendered, 26 pages; cross-links based to `/portfolio/notes/honest-automation` + `/portfolio/notes/nucleus-byo-keys` + `/portfolio/notes/nucleus-local-mode` with zero bare `/notes/`; Key takes block + date rendered). No video requested. NOT DEPLOYED — the lead does the hard confidentiality + no-dollar-figures sweep + deploy.

## 2026-08-10 loop-run | HELD for direction — Nucleus shipped new work; queue drained; asked owner which new area to cover
- Queue writeable = 0 (only `ychack`, needs owner input). Per runbook, checked `git -C ~/Projects/nucleus log` for new coverable mechanisms since the 4 shipped deep-dives (HEAD moved Jul 2 → Aug 9, b3fb92f).
- Groundable, non-confidential candidates found (NEW areas, not the pre-steered four — surfaced to owner rather than auto-writing into new client-adjacent scope): (1) weekly AGENTIC QA EXPLORER (Claude + Playwright MCP autonomously tests the live app: sign in as demo user, upload/ask/delete, self-verify + self-clean, capped paid asks, honest report) — strongest agent-systems fit; (2) DeepAgents/LangGraph HARNESS MIGRATION (mostly roadmap + one shipped spike so far — wait for more); (3) customer DOCS SITE (/docs overview + getting-started). 
- Did NOT write — asked owner to pick (Telegram + session). NEVER touch the client-identifying adjacent commits (the client's Excel / billing figures / call-log).
