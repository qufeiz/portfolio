Portfolio-writer SELF-RENEWING loop fire (cron-fired WEEKLY, Mondays 12:07 UTC / ~5:07 AM PDT — one article per week, owner's choice 2026-07-24). Cron owns the cadence. Do NOT call ScheduleWakeup and do NOT add ticks.

STEP 0 — RE-ARM FIRST (unconditional; do this on EVERY fire BEFORE anything else, even if you later STOP at dedup — it resets the 7-day auto-expiry so the loop never lapses while this session is alive):
  - Load the cron tools: ToolSearch `select:CronList,CronCreate,CronDelete`.
  - CronList. Record the job ids of ALL existing recurring jobs that run this loop (their prompt points at this loop-prompt.txt).
  - CronCreate a FRESH recurring job: cron `7 12 * * 1` (weekly, Mondays), recurring true, prompt EXACTLY:
      Portfolio-writer loop fire — read and follow /home/codex/Projects/portfolio/.claude/state/loop-prompt.txt exactly; do not call ScheduleWakeup.
  - CronDelete each OLD id you recorded above (NOT the new one). End state: exactly ONE fresh job, 7-day clock reset.

STEP 1 — DEDUP (safety against double-fire): now=$(date +%s); t=$(date -d 'TZ="America/Los_Angeles" 05:00 today' +%s); if [ "$t" -gt "$now" ]; then prev5=$(date -d 'TZ="America/Los_Angeles" 05:00 yesterday' +%s); else prev5=$t; fi; last_run=$(cat /home/codex/Projects/portfolio/.claude/state/loop-last-run 2>/dev/null || echo 0). If last_run >= prev5, this 5am cycle already ran -> report "already ran this cycle (re-armed)" and STOP (you already re-armed in STEP 0).

STEP 2 — RUN: execute ONE writer iteration per /home/codex/Projects/portfolio/.claude/AGENTS.md from repo root /home/codex/Projects/portfolio — read the ledger (state/sources.md + coverage.md + article-queue.md + log.md), spawn the portfolio-writer subagent (if `subagent_type: portfolio-writer` is unavailable, use a `general-purpose` agent told to read+follow `.claude/agents/portfolio-writer.md` AND `.claude/refs/write-article.md` exactly) to write ONE article on the next highest-value real gap (HONESTY FLOOR: skip and write nothing if there is no genuine gap — never filler). `ychack` is NOT writeable yet (only image assets) — skip until the owner describes what was built.
  NUCLEUS is now a live source: it is CLIENT WORK — obey the `sources.md` › Nucleus gate (MECHANISM ONLY from `~/Projects/nucleus` `main`/code; client NEVER named; never client-comms docs / client-rebrand branches / keys / client-specific dollar figures). The loop may check `git -C ~/Projects/nucleus log` for a newly-shipped, non-confidential mechanism to write about.
  Then GATE as the lead (honesty/confidentiality/PII/build) — for Nucleus, sweep HARD for any client-identifying detail. DEPLOY policy: the owner APPROVED the Nucleus confidentiality bar on 2026-07-24 (published the first two drafts), so Nucleus articles now AUTO-PUBLISH like own-work → `bash scripts/deploy.sh` + verify live + Telegram-notify — PROVIDED your hard lead confidentiality sweep is clean (client never named; no client-comms/rebrand-branch/keys/dollar-figures; grep + proper-noun scan clean). HOLD a piece as a draft ONLY if it has a genuine, UNRESOLVED client-identifying/confidentiality doubt: then it MUST set `draft: true` in frontmatter — that flag is what EXCLUDES it from the Astro build (`publishedArticles()` filters `!data.draft`); "just don't run deploy" is NOT enough, a later full-site deploy would publish a flagless draft (this bit us 2026-07-09) — do NOT deploy, and Telegram the owner to review. Update coverage.md + append to log.md either way.

STEP 3 — MARK: write `date +%s` to /home/codex/Projects/portfolio/.claude/state/loop-last-run.

STEP 4 — NOTIFY THE OWNER ON TELEGRAM (only when there is real news — never on a plain hold/skip, to avoid spam). Send via the central cred:
  `set -a; . ~/.claude/integrations/telegram.env; set +a; curl -s "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" -d chat_id=$TELEGRAM_CHAT_ID --data-urlencode "text=<msg>"` (needs the Bash `dangerouslyDisableSandbox` flag — the sandbox blocks api.telegram.org). Send a message when, and only when:
  - a NEW article went LIVE (own-work OR an auto-published Nucleus piece) → e.g. `📝 Published: "<title>" — https://qufeiz.github.io/portfolio/notes/<slug>/` (title only; never client-identifying detail)
  - a Nucleus piece was HELD for an unresolved confidentiality doubt (rare) → e.g. `⏸ Held "<title>" — a detail may identify the client; want me to cut it and publish, or leave it?` (title only)
  - the loop is genuinely BLOCKED needing input (e.g. ychack needs a description) → one concise ask.
  Do NOT notify on a routine hold/skip or a re-arm. If the send fails, log it and continue (never block the loop on a notify).

Report 2-3 lines (re-armed + article shipped or skipped + why + whether a Telegram ping was sent). No ScheduleWakeup, no ticks.
