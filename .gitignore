# build output
dist/
# generated types
.astro/
# deps
node_modules/
# logs
npm-debug.log*
# env
.env
.env.production
# os
.DS_Store
# temp/scratch output (screenshots, capture scripts) — keep the dir, ignore contents
.scratch/*
!.scratch/.gitkeep

# installed AI agent skills (Hyperframes etc.) — tooling, not source
.agents/
